import { test } from "../../fixtures/auth.fixture";

test.describe("Checkout details page", () => {
  test("should display the 'Checkout: Your Information' page title", async ({}) => {});

  test("should show the cart badge with the correct number of items", async () => {
    // Test implementation
  });

  test("should add correct address information", async () => {
    // Test implementation
  });

  test("should display error message for missing first name", async () => {
    // Test implementation
  });

  test("should display error message for missing last name", async () => {
    // Test implementation
  });

  test("should display error message for missing postal code", async () => {
    // Test implementation
  });

  // should display cancel and continue buttons
  test("should display cancel and continue buttons", async () => {
    // Test implementation
  });

  //
});

# CLAUDE.md

## Project

JavaScript + Playwright Test E2E automation for **saucedemo.com**.

- Package manager: **pnpm** only (`pnpm ^11.18.0`)
- Node: **22.x LTS**
- Pattern: **Page Object Model (POM)**
- Test data: `test-data/` using `.json` / `.js`
- API testing: out of scope

## Commands

```bash
pnpm install
pnpm exec playwright install --with-deps chromium
pnpm exec playwright test
pnpm exec playwright test tests/ui/login.spec.js
pnpm exec playwright test -g "test name"
pnpm exec playwright test --grep @smoke
pnpm exec playwright test --list
pnpm exec playwright test --debug
pnpm run lint
pnpm run lint:fix
pnpm run format:check
pnpm run format
pnpm run report
pnpm run report:allure
```

Do not use `npm` or `yarn`.

## Current Structure

```text
pages/                 # Page Objects
tests/ui/              # Single-page UI tests
tests/e2e/             # Multi-page/user-journey tests
tests/auth/            # Auth setup specs (storageState generation)
fixtures/              # Custom Playwright fixtures
test-data/             # Test data
utils/                 # Framework-agnostic helpers
auth/                  # Generated storageState.json (gitignored contents may vary)
allure-results/        # Raw Allure results (gitignored, generated by playwright test)
allure-report/         # Rendered Allure HTML report (gitignored, generated on demand)
docs/                  # Reference docs and test cases
.github/workflows/     # CI
playwright.config.js   # Global Playwright config
```

Current implementation:

- `pages/login.page.js` — implemented
- `pages/inventory.page.js` — implemented; also exposes cart-related helpers (`addProductToCart`, `removeProductFromCart`, `cartProductCount`, `productButton`, `openCart`) and `logout` (opens the header menu and clicks the logout link). `menuBtnLoc` is role-based (`getByRole("button", { name: "Open Menu" })`) rather than `getByTestId("open-menu")` — that test id sits on a decorative `<img>` that the wrapping button visually intercepts clicks for
- `pages/cart.page.js` — implemented; exposes `pageTitle`, `getCartItemCount`, `getAllProductNames`, `getAllProductPrices`, `getAllProductDescriptions`, `productQuantity`, `removeProductFromCart`, `continueShoppingButton`, `checkoutButton`, `checkout`
- `pages/checkout.page.js` — implemented (checkout-step-one / "Checkout: Your Information"); exposes `pageTitle`, `fillInformation`, `continueToOverview`, `cancel`, `getErrorMessage`
- `pages/payment.page.js` — implemented (checkout-step-two / "Checkout: Overview"); exposes `pageTitle`, `getCartItemCount`, `getAllProductNames`, `getAllProductPrices`, `getAllProductDescriptions`, `productQuantity`, `paymentInformation`, `shippingInformation`, `itemTotal`, `tax`, `total`, `finish`, `cancel`
- `pages/checkout-complete.page.js` — implemented ("Checkout: Complete!" order confirmation page); exposes `pageTitle`, `confirmationIcon`, `confirmationHeader`, `confirmationText`, `backHomeButton`, `backHome`
- `tests/ui/login.spec.js` — implemented; imports `pages.fixture` so it stays anonymous
- `tests/ui/inventory.spec.js` — implemented; authenticated via `auth.fixture`. No longer performs a UI login, so it is now genuinely a single-page UI test
- `tests/ui/cart.spec.js` — implemented; authenticated via `auth.fixture`. `"Cart"` describe block adds a product via `InventoryPage`; `"Cart page"` describe block drives `InventoryPage.openCart()` then asserts against `CartPage` (title, item names/prices, quantity, Continue Shopping/Checkout buttons)
- `tests/ui/payment.spec.js` — implemented; authenticated via `auth.fixture`. Drives `InventoryPage` → `CartPage.checkout()` → `CheckoutPage.continueToOverview()` to reach the overview page, then asserts against `PaymentPage` (item list, shipping/payment info, currency formatting). The floating-point item-total defect case uses `test.fail()` to document a known Sauce Demo bug
- `tests/ui/checkout-complete.spec.js` — implemented; authenticated via `auth.fixture`. A shared `test.beforeEach` drives the full journey through `PaymentPage.finish()`, then each test asserts one piece of `CheckoutCompletePage` (title, icon, heading, message, Back Home button)
- `tests/e2e/auth.spec.js` — implemented; imports `auth.fixture` (authenticated by default) so the logout test can use `inventoryPage` directly. The `"unauthenticated access"` nested describe overrides `storageState` to an empty session (`test.use({ storageState: { cookies: [], origins: [] } })`) for the one test that needs to start signed out
- `tests/auth/auth.setup.js` — implemented; logs in as `standard_user` and writes `auth/storageState.json`. Wired as the `setup` project in `playwright.config.js`, which `chromium` declares via `dependencies: ["setup"]`
- `tests/e2e/checkout.spec.js` — implemented; covers the 15 E2E scenarios (E2E-01–E2E-15) from `docs/test-cases/01-automation-testcases.md`'s E2E table. `"Checkout @e2e"` (authenticated via `auth.fixture`) covers E2E-01–E2E-08; `"Checkout @e2e - anonymous users"` overrides `storageState` to an empty session for the locked-out-user and `problem_user` cases (E2E-09, E2E-12); `"Checkout @e2e - session behavior"` covers logout/refresh/multi-purchase/full-journey cases (E2E-10, E2E-11, E2E-13–E2E-15). E2E-12 documents a real Sauce Demo defect: for `problem_user`, the checkout-information first/last name fields collide, so `continueToOverview()` is blocked by the "Last Name is required" validation error instead of reaching the overview page
- `fixtures/pages.fixture.js` — implemented; page object fixtures only (`loginPage`, `inventoryPage`, `cartPage`, `checkoutPage`, `paymentPage`, `checkoutCompletePage`), no authentication. `cartPage`, `checkoutPage`, `paymentPage`, and `checkoutCompletePage` do not `goto()` — each is a click-through page reached from the previous step (`inventoryPage.openCart()` → `cartPage.checkout()` → `checkoutPage.continueToOverview()` → `paymentPage.finish()`)
- `fixtures/auth.fixture.js` — implemented; extends `pages.fixture` with the `storageState` option override
- `utils/common.utils.js` — implemented
- `storageState` — implemented at fixture level via `auth.fixture.js`; generated automatically by the `setup` project before every run
- Visual regression — not implemented
- Allure — implemented; `allure-playwright` reporter writes `allure-results/`, `allure-commandline` (CLI) renders `allure-report/`. `pnpm run report:allure` generates + opens locally; CI generates and uploads it as a build artifact. See `docs/frameworks/13-allure-report-setup.md`
- Jenkins / SonarCloud — not implemented

**Always verify the actual filesystem/config before assuming a feature exists.** Documentation may describe planned or historical behavior.

## Architecture Rules

### Page Objects

- One class per page: `<page>.page.js` → `class PageName`
- Export the class as default.
- Constructor receives and stores Playwright `page`.
- Keep locators in the page object; tests must not call `page.locator()` directly.
- Prefer `getByRole()` / `getByTestId()`; CSS is acceptable when necessary.
- Locator properties use the `Loc` suffix: `usernameLoc`, `loginButtonLoc`.
- Page objects expose user actions/assertion helpers such as `login()`, `goto()`, `addProductToCart()`.

### Tests

- `tests/ui/`: validates one page in isolation.
- `tests/e2e/`: spans multiple page objects or represents a complete user journey.
- `tests/auth/`: auth setup specs named `<name>.setup.js` (matched by the `setup` project) that write `storageState` to `auth/`.
- Use page objects and fixtures rather than raw selectors.
- Import test data instead of hardcoding reusable values.
- Use Playwright's retrying assertions for live UI state.

### Fixtures

- Located in `fixtures/`, named `<name>.fixture.js`.
- Extend Playwright `base` with `test.extend()`.
- Re-export `{ test, expect }`.
- **Name a fixture after the object it provides, not the action it performs** — `inventoryPage`, not `loginFixture`. No `Fixture` suffix on the export; `.fixture` belongs to the filename only.
- Avoid side-effect-only fixtures that end in `await use()` with no argument. If a fixture provides nothing, make it an auto fixture (`{ auto: true }`) instead of adding an unused parameter to every test.
- `fixtures/pages.fixture.js` — all page object fixtures, deliberately **without** auth. Add new page objects here rather than creating one fixture file per page: a spec can only import one `test` object, so multi-page e2e specs need them together.
- `fixtures/auth.fixture.js` — extends `pages.fixture` with the `storageState` override. **Importing it is the opt-in to a logged-in session**; specs should not repeat `test.use({ storageState: ... })`.
- Specs that must start anonymous (login, locked-out, invalid credentials) import `pages.fixture` instead.
- A page fixture may `goto()` only when the page is a direct-URL entry point; for pages reached by clicking through the UI, let the test drive navigation.
- Rationale and trade-offs: `docs/frameworks/12-fixtures-and-storage-state.md`.

### Test Data

- Located in `test-data/`.
- JSON: `<page>.data.json` (e.g. `inventory-page.data.json`).
- JS: `<page>.data.js` when logic/structure is required (e.g. `users.data.js`, `login-page.data.js`, `inventory-sort.data.js`).
- Prefer data files over inline test literals.

### Utils

- Located in `utils/`, named `<name>.utils.js`.
- Use for pure, framework-agnostic helpers.
- Do not put Playwright/page/locator logic in utils.

## Naming

- Files: lowercase kebab-case — `login.page.js`, `common.utils.js`
- Variables/methods/properties: camelCase
- Classes: PascalCase
- Locators: camelCase + `Loc`
- Page objects: `<page>.page.js`
- Specs: `<feature>.spec.js`
- Fixtures: `<name>.fixture.js`
- Utils: `<name>.utils.js`

## Test Naming

Prefer:

```text
should <expected behavior> with <data>
should <expected behavior> when <action>
should <expected behavior> when <action> with <data>
should <expected behavior>
```

Examples:

```text
should login with valid credentials
should display error message when submitting empty form
should add product to cart when clicking on add to cart button
should display products list
```

## Playwright Config

`playwright.config.js` currently:

- Uses `https://www.saucedemo.com/` as default `baseURL`
- Uses `data-test` as `testIdAttribute`
- Loads `.env`
- Supports `BASE_URL`, `HEADLESS`, `RETRIES`, and `WORKERS`
- Defaults to headless mode
- Uses HTML reporter + Allure reporter (`allure-playwright`, output to `allure-results/`)
- Uses `trace: "on-first-retry"`
- Enables **Chromium only**
- Firefox/WebKit/mobile projects are commented out
- Has **no** `snapshotDir`; visual testing is not configured

Do not enable additional browsers or introduce visual testing without checking the current project requirements.

## CI

`.github/workflows/playwright.yml` currently:

1. Runs lint + Prettier checks.
2. Runs tests after lint succeeds.
3. Installs Chromium only.
4. Runs headless.
5. Uploads the HTML report.
6. Uploads `test-results/` on failure.
7. Generates and uploads the Allure report (`allure-report/`).
8. Triggers on pushes to `main`; PR trigger is currently commented out.

Do not assume other workflows exist. Verify `.github/workflows/` first.

## Test Users

Defined in `test-data/users.data.js`:

- `standard_user` — valid
- `locked_out_user` — invalid/locked
- `problem_user` — invalid; error message currently empty
- `wrong_username` — invalid username
- `wrong_password` — invalid password
- `wrong_both` — invalid username/password

Password is generally `secret_sauce`.

## Important Gotchas

- Use `toEqual()` for already-resolved static arrays/values.
- Prefer `toSorted()` when sorting reusable arrays to avoid mutation.
- Import filenames exactly, including `.page`, `.utils`, and `.fixture` segments:
  `../utils/common.utils`, not `../utils/common`.
- Do not trust `docs/frameworks/` as proof that something is implemented; verify source/config/workflows.
- Visual baselines are OS-specific; generate them on the same OS as the test runner.
- Check `.vscode/playwright.code-snippets` for existing Playwright scaffolding snippets.

## Documentation

`docs/frameworks/01–07` are primarily reference/target-design documents.
`docs/frameworks/08–11` contain ratings, implementation notes, and historical/design information. Verify current code before treating them as authoritative.
`docs/frameworks/12-fixtures-and-storage-state.md` documents the current auth/fixture design and the naming rules that go with it.
`docs/frameworks/13-allure-report-setup.md` documents the Allure reporting setup (reporter + CLI, scripts, CI wiring).

`docs/test-cases/` contains the project's actual test-case documentation.

## Agent Rules

1. Inspect the existing implementation before creating or modifying files.
2. Follow the architecture and naming rules above.
3. Reuse existing page objects, fixtures, utilities, and test data.
4. Do not introduce unnecessary framework abstractions.
5. Keep tests readable and focused on behavior.
6. Run relevant tests/lint/format checks after changes.
7. When implementing a feature, update this file's **Current Structure** only if the project state actually changed.
8. Keep `CLAUDE.md`, `AGENTS.md`, and `copilot-instructions.md` synchronized when instruction changes are intentional.
